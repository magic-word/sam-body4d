# cuda/ assets

Put the NVIDIA §4.1 Prerequisites screenshot here as `nvidia_prereq_4_1.png`
(referenced by `worked_example_source_embedding.md`, Representation 2).

This folder holds source-excerpt screenshots for the CUDA-on-Windows topic. Small PNGs are committed
to git; large media is git-ignored and mirrored to GCS (see `dev_environment_and_data.md`).
